<UserControl x:Class="WindLightSimluator.Views.Components.Base.BorderText"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WindLightSimluator.Views.Components.Base"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:conv="clr-namespace:WindLightSimluator.Converters"
             
              x:Name="root">
    <UserControl.Resources>
        <conv:HeightToFontSizeConverter x:Key="HeightToFontSizeConverter"/>

    </UserControl.Resources>

    <Border x:Name="mainBorder" 
            BorderBrush="{Binding BorderColor,RelativeSource={RelativeSource AncestorType=UserControl}}"
            BorderThickness="{Binding BorderThicknessValue,  RelativeSource={RelativeSource AncestorType=UserControl}}"
            Background="{Binding  BackgroundColor, RelativeSource={RelativeSource AncestorType=UserControl}}"
            VerticalAlignment="Stretch"
            HorizontalAlignment="Stretch"
            Margin="0"
            >
      
        <Grid  HorizontalAlignment="Stretch"  VerticalAlignment="Center" >
            <!-- 右下角小字 -->
            <TextBlock  Text="{Binding SubText, RelativeSource={RelativeSource AncestorType=UserControl}}"
             FontSize="{Binding SubTextFontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
             Foreground="{Binding SubTextColor, RelativeSource={RelativeSource AncestorType=UserControl}}"
             HorizontalAlignment="Right"
             VerticalAlignment="Bottom"
             FontFamily="Segoe UI"
            />
            <TextBlock 
                       x:Name="mainTextBlock"
                       Text="{Binding MainText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontSize="{Binding MainTextFontSize,RelativeSource={RelativeSource AncestorType=UserControl}}"      
                       Foreground="{Binding MainTextColor, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       TextAlignment="Center"
                       FontFamily="Segoe UI"
                
                       />

          
        </Grid>

    </Border>

</UserControl>


<!--Margin="{Binding BorderThicknessValue, ElementName=root}"-->

<!-- 主文字（可选：注释掉 Viewbox 可以取消自适应） -->
<!--<Viewbox Stretch="Uniform"
               Visibility="{Binding MainTextAutoFit, ElementName=root}">
          <TextBlock Text="{Binding MainText, ElementName=root}"
                     Foreground="Black"
                     TextAlignment="Center"/>
      </Viewbox>-->

<!-- 主文字(非自适应) -->


<!--<Grid.ColumnDefinitions>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>-->
<!--x:Name="mainTextBlock"-->
<!--Text="{Binding MainText, ElementName=root}"
                     FontSize="{Binding MainTextFontSize, ElementName=root}"-->


<!--FontSize="{Binding MainTextFontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"-->

<!--FontSize="{Binding ActualHeight, ElementName=mainBorder, Converter={StaticResource HeightToFontSizeConverter}}"-->      
    